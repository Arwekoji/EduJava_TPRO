package base;

/**
 * Точка входа в программу по теме 4.2
 *
 * @author Артур Федьвереш
 */
public class Main {

    /**
     * Точка входа в программу
     *
     * @param args Аргументы входа в программу
     */
    public static void main(String[] args) {
        Book.testClass(); // 1 задание

        printLine();

        Circle.testClass(); // 2 задание

        printLine();

        Matrix.testClass(); // 3 задание

        printLine();

        Config.testClass(); // 4 задание
    }

    /**
     * Метод-разделитель для оформления выводов разных задач
     */
    public static void printLine() {
        System.out.println();
        System.out.println("- - - - - - - - - - - -");
        System.out.println();
    }
}


/* Задание 4.2 — Конструкторы

Задача 1. Класс Book.
Создайте класс Book с полями: название (String), автор (String), год издания (int), количество страниц (int), цена (double). Реализуйте четыре конструктора:
первый принимает только название и автора, второй добавляет год, третий добавляет страницы, четвёртый принимает все поля.
Используйте цепочку this() чтобы не дублировать код инициализации. Добавьте метод printInfo и метод isClassic возвращающий true если книга издана до 1950 года.
Создайте по одному объекту каждым конструктором и выведите информацию.

Задача 2. Класс Circle.
Создайте класс Circle с полями radius (double), centerX (double), centerY (double). Реализуйте конструкторы:
без параметров создаёт единичную окружность в начале координат, с одним параметром принимает радиус, с тремя параметрами принимает радиус и координаты центра,
копирующий конструктор принимает другой Circle. Реализуйте методы: area, perimeter, contains принимает координаты точки и возвращает true если точка внутри окружности,
intersects принимает другой Circle и возвращает true если окружности пересекаются (расстояние между центрами меньше суммы радиусов). Проверьте все методы.

Задача 3. Класс Matrix.
Создайте класс Matrix с полями rows (int), cols (int), data (двумерный массив int[][]). Реализуйте конструкторы: принимает rows и cols и создаёт нулевую матрицу,
принимает готовый двумерный массив и копирует его (не ссылку, а содержимое), копирующий конструктор. Добавьте методы: get принимает i и j возвращает элемент,
set принимает i j и значение, printMatrix выводит матрицу в табличном виде, add принимает другую Matrix и возвращает новую матрицу-сумму,
multiply принимает другую Matrix и возвращает произведение. Проверьте что копирующий конструктор создаёт независимую копию.

Задача 4. Класс Config.
Создайте класс Config для хранения настроек приложения. Поля: host (String), port (int), maxConnections (int), timeout (int), debug (boolean).
Используйте блок инициализации для установки значений по умолчанию: host = "localhost", port = 8080, maxConnections = 100, timeout = 30, debug = false.
Реализуйте конструктор без параметров (использует только блок), конструктор принимающий host и port, полный конструктор со всеми полями.
Добавьте метод printConfig и статическое поле instanceCount считающее количество созданных конфигураций.
Продемонстрируйте что блок инициализации выполняется при каждом создании объекта.
 */

/*Критерии оценки:

- цепочка this() используется корректно — конструкторы не дублируют код
- копирующий конструктор создаёт полностью независимую копию
- блок инициализации в Config выполняется перед каждым конструктором
- метод intersects в Circle основан на расстоянии между центрами через Math.sqrt
 */