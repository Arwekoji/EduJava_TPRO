package base;

import assist.*;

/**
 * Точка входа в программу по теме 4.4
 *
 * @author Артур Федьвереш
 */
public class Main {
    /**
     * Точка входа в программу по теме 4.4
     *
     * @param args Аргументы точки входа
     */
    public static void main(String[] args) {
        CodeMetrics.setTimer();

        AdvancedText.printTaskSeparator(1, "Student");
        Student.testClass();
        /* Задача 3:

        identityGroup в коде класса - представление оригинальной реализации hashCode и equals в обход переопределения.
        Изначально equals сравнивает не по содержимому, а по адресу в памяти (ссылке).

        Ручная реализация, в рамках контракта, использует фиксированный набор правил переопределения. Они заключаются в следующих принципах:
        1. Рефлексивность ( a.equals(a) )
        2. Симметричность (Одинаковые методы и поля)
        3. Транзитивность (Если A = B и B = C, то и A = C)
        4. null -> false (Т.е. превращение null в false)

        При переопределении обязательно используются все эти принципы, а также аннотация @Override.
        Можно автоматически сгенерировать простое переопределение equals, hashCode и toString методов, используя функцию Generate в InteliJ IDEA

        Также отмечу, что при ручном переопределении если был переопределен equals(), то должен быть переопределен и hashCode()
         */

        CodeMetrics.printTimer();
    }
}

/*Задание 4.4 — equals, hashCode, toString

### Задача 1
- Создайте класс `Student` (name, age, email).
- Переопределите equals (по name + email), hashCode и toString.
- Продемонстрируйте: два Student с одинаковыми данными → equals=true, HashSet.size()=1.

### Задача 2
- Создайте HashSet<Student>,
- добавьте 5 студентов (2 дубликата).
- Покажите что size=3.
- Уберите переопределение hashCode — покажите что size=5 (дубликаты не распознаются).

### Задача 3
- Сгенерируйте equals/hashCode через IntelliJ (Alt+Insert).
- Сравните с ручной реализацией.

 */

/* ОЖИДАЕМЫЙ РЕЗУЛЬТАТ
- equals сравнивает name+email, hashCode использует Objects.hash
- HashSet корректно распознаёт дубликаты с обоими переопределениями
- Без hashCode дубликаты не распознаются — объяснено почему
 */